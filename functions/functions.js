function scream (s) {
    console.log(s.toUpperCase() + "!!!");
}

scream("there's always money in the banana stand");


const yell = (s) => 
    console.log(s.toUpperCase() + "!");

yell("there's always money in the banana stand");
